# CSD Config
set imap_user = "csd4040"
set imap_pass = ""
set my_user   = "csd4040"
set from      = "csd4040@csd.uoc.gr"
set realname  = "csd4040"

set folder    = "imaps://mailhost.csd.uoc.gr:993"
set smtp_url  = "smtps://$imap_user:$imap_pass@mailhost.csd.uoc.gr:465"
mailboxes = +INBOX +Sent +Drafts +Trash

# General config
set spoolfile    = "+INBOX"
set record       = "+Sent"
set postponed    = "+Drafts"
set trash        = "+Trash"
set sort         = "reverse-threads"
set sort_aux     = last-date-received
set header_cache = "~/.cache/neomutt/neomutt"
set sleep_time   = 0
set include      = yes 
set use_from     = yes
set send_charset ="utf-8"


# Keybindings
bind attach,index g first-entry
bind attach,index G last-entry
bind  generic        l select-entry
bind  index          l display-message
macro index          h '<change-folder>?'
bind  index,pager    H display-toggle-weed
bind  pager          h exit
bind  pager          j next-line
bind  pager          k previous-line
bind  pager          l view-attachments
bind  attach,compose l view-attach
bind  attach         h exit
bind  attach         H display-toggle-weed
bind  compose        l view-attach
bind index           za  collapse-thread
bind index           zA  collapse-all
bind index,pager B sidebar-toggle-visible
source lukecolor.muttrc
